    ; load this code into $9000    ; тHE FOLLOWING IS COMPATIBLE WITH ць16ассемблер    ; YOU CAN "X" AND RUN FROM WITHIN ць16ASSEMBLER    ; OR YOU CAN сыс $9000 AFTER лоад"SAMPLE",8,1        цхроут = $ффд2            .ORG $9000            ;clear screen    LDA #$93    JSR цхроут    ;put A in the left top corner    LDA #$41    JSR цхроут    LDA #$00    STA $9F25   ;VERA CTRL    ;Set vera address to read from    LDA #$00                ;low    STA $9F20    LDA #$B0                ;middle    STA $9F21    LDA #$21                ;high    STA $9F22    LDA $9F23   ;read character     PHA    ;set address to next line    LDA #$00                ;low    STA $9F20    LDA #$B1                ;middle    STA $9F21    LDA #$21                ;high    STA $9F22    PLA    STA $9F23   ;put character on the screen    LDA #$0D    ;move cursor one line down, else the A will be overwritten with Ready    JSR цхроут            RTS         ; ASSUMES WE GOT HERE BY A сыс CALL        