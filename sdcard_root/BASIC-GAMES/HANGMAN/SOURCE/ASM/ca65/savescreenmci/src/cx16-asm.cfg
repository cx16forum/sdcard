FEATURES {
    STARTADDRESS: default = $0700;
}
MEMORY {
    MAIN:     file = %O, start = %S,     size = $0100;
}
SEGMENTS {
    CODE:     load = MAIN,     type = ro;
    BSS:      load = MAIN,     type = bss, define = yes;
}
